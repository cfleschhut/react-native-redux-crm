import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

function AddPerson() {
  return (
    <View>
      <Text>AddPerson screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({});

export default AddPerson;
